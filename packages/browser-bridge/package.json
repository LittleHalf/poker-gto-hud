{
  "name": "poker-browser-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && npm run copy-manifest",
    "copy-manifest": "cp manifest.json dist/manifest.json",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.299",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
